/**
 * Shared type re-exports
 * 
 * Barrel file for all shared TypeScript types.
 * Import from '@/types' for clean imports across the application.
 */

export type { NavItem, NavGroup, Theme, SidebarState } from './navigation';
